executable = test-env.sh
output = test-$(ClusterId).$(ProcId).out
error = test-$(ClusterId).$(ProcId).err
log = test-$(ClusterId).$(ProcId).log

environment = SOFT_DIR=/software/dteam; OTHER_DIR=/nfs/dteam

queue

